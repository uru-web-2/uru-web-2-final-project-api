// Query to load the modules
export const LOAD_MODULES_FN = "SELECT * FROM load_modules()"

// Query to load the objects
export const LOAD_OBJECTS_FN = "SELECT * FROM load_objects()"

// Query to load the methods
export const LOAD_METHODS_FN = "SELECT * FROM load_methods()"

// Query to load the profiles
export const LOAD_PROFILES_FN = "SELECT * FROM load_profiles()"

// Query to load the permissions
export const LOAD_PERMISSIONS_FN = "SELECT * FROM load_permissions()"